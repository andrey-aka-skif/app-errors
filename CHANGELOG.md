# Журнал изменений

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).
Этот проект придерживается [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [3.3.0] - 2026-09-02

Выпуск чинит вход для `require` и разводит вторую сборку на две: одна для
CommonJS, другая для браузера. Публичное API не изменилось.

### Добавлено

- Подпуть `./browser` ведёт на браузерную сборку `dist/index.iife.js`. Она
  подключается тегом `script` и объявляет глобаль `appErrors`.

### Исправлено

- Точка входа для `require` больше не пуста. `exports.require` вёл на
  `dist/index.umd.js`, а при `"type": "module"` Node читает `.js` как ESM:
  UMD-обёртка не отрабатывала, и `require('@andrey-aka-skif/app-errors')`
  возвращал ноль экспортов — молча, без ошибки. `import` работал штатно.

### Изменено

- Вторая сборка расщеплена на CommonJS и браузерную, состав `dist` изменился:
  вместо `index.es.js` и `index.umd.js` выпускаются `index.js`, `index.cjs` и
  `index.iife.js`. Потребителя, который обращается к пакету по имени, это не
  задевает: точку входа выбирает карта `exports`.

## [3.2.3] - 2026-08-11

### Исправлено

- Dist-тег линии сопровождения переименован из `v3-lts` в `lts-v3`. Реестр
  Gitea читал прежнее имя как версию и отвергал публикацию с
  `400 The tag name is invalid`, уже приняв пакет: версия оказывалась в
  реестре, а прогон падал. Каждый выпуск тройки упирался в это.
- Имя dist-тега проверяется до сборки, а не выясняется отказом реестра на
  последнем шаге.

### Изменено

- Публикация запускается только релизом: ручной запуск воркфлоу убран вместе
  с меткой `replay`. Переиздать выпущенную версию реестр всё равно не даёт, а
  упавший прогон переигрывается кнопкой Re-run.

### Добавлено

- `CONTRIBUTING.md` — устройство репозитория и правила выпуска.

## [3.2.2] - 2026-08-09

### Изменено

- Год в `LICENSE` приведён к диапазону 2025-2026.
- Readme: в шапке проговорен статус линии, установка показана через dist-тег
  `v3-lts`. Без тега `npm install` ставит актуальную линию 4.x.

## [3.2.1] - 2026-08-09

Выпуск линии сопровождения: код пакета не изменился, правки затрагивают
только публикацию и проверку сборки.

### Изменено

- Dist-тег при публикации определяется сравнением тега релиза со старшим
  тегом репозитория: старший идёт в `latest`, патч линии сопровождения — в
  тег вида `v3-lts`, предрелиз — в `next`. Раньше `latest` был зашит жёстко,
  и выпуск патча 3.x после 4.0.0 увёл бы `latest` на тройку.
- Воркфлоу публикации переименован из `release.yml` в `publish.yml`, файлы
  для GitHub и Gitea приведены к общему виду.
- Дымовая проверка `verify:dist` гоняет обе сборки — ES и UMD, а не только
  ES.

## [3.2.0] - 2026-08-08

### Исправлено

- Билдер `fromSuperagent` терял `detail` для статуса 500, в отличие от всех
  остальных статусов и от `fromAxios`.
- Билдеры `fromAxios` и `fromSuperagent` выбрасывали `TypeError` на `null`,
  `undefined` и примитивах. Теперь любой непригодный аргумент даёт
  `UnknownError`: билдер вызывается в `catch` и не должен падать сам.
- Классы ошибок не задавали `name` — у всех экземпляров он был равен `Error`.
  Теперь `error.name` совпадает с именем класса и попадает в стектрейс.

### Изменено

- `ERROR_TYPE` заморожен: попытка изменить или дополнить его выбрасывает
  `TypeError`.
- Относительные импорты внутри `src/` дополнены расширением `.js`.
- Удалён недостижимый код в `fromSuperagent`: проверка `hasServerError`
  всегда истинна после проверки на потерю соединения.

### Добавлено

- Тесты билдеров: 74 проверки таблиц статусов, извлечения `details` и веток
  обрыва связи. До этого логика разбора ответов не проверялась вовсе.
- `npm run verify:dist` — дымовая проверка собранного пакета. Юнит-тесты
  гоняются по исходникам и не видят того, что со сборкой делают бандлер и
  минификатор. Выполняется хуком `postbuild`, то есть при каждой сборке, а
  не только в CI.
- Проверка форматирования выполняется хуком `prebuild`: неотформатированный
  код не соберётся.

## [3.1.6] - 2025-12-27

Запись сводит выпуски 3.1.4, 3.1.5 и 3.1.6: все три содержали изменения
только в конфигурации CI/CD и не затрагивали код пакета.

### Изменено

- Параметризованный запуск сборки.
- Переход на PAT вместо автоматического токена репозитория.
- Исправление адресов реестров публикации.

## [3.1.3] - 2025-12-20

### Добавлено

- Добавлен CI/CD workflow для Github и Gitea
- В CI/CD добавлено явное указание npm тега `latest`

### Изменено

- Обновлены конфигурационные файлы проекта
- Версионирование пакетов теперь происходит на основе git тегов в CI/CD workflow

## [3.1.0] - 2025-07-28

### Добавлено

- Типизированная ошибка `CustomError`.
- Юнит-тесты для `CustomError`.
- Документация по ошибке `CustomError` в `README.md`.

## [3.0.0] - 2025-07-28

### Изменено

- Версия 3.0.0-alpha.2 признана стабильной. Функциональных изменений нет.

## 3.0.0-alpha.2 - 2025-07-28

_Версия не помечена тегом, ссылки на сравнение для неё нет._

### Добавлено

- Типизированные ошибки:
  - `BadRequestError`,
  - `ConflictError`,
  - `DisconnectedError`,
  - `ForbiddenError`,
  - `InternalServerError`,
  - `LogicError`,
  - `NotFoundError`,
  - `UnauthorizedError`,
  - `UnknownError`.
- Билдер типизированных ошибок на основе библиотеки `axios`.
- Билдер типизированных ошибок на основе библиотеки `superagent`.
- Документация по кастомным ошибкам в `README.md`

### Изменено

- **BREAKING CHANGE**: Классы ошибок больше не содержат логику парсинга ответов (перенесено в билдеры).
- **BREAKING CHANGE**: Удалена прямая зависимость от конкретных HTTP-библиотек (теперь используются билдеры).
- **BREAKING CHANGE**: Вместо обобщенного класса ошибки добавлен набор типизированных ошибок.

### Удалено

- Устаревшие классы `AppErrorViaAxios` и `AppErrorViaSuperagent` (заменены на билдеры).

## [2.0.0] - 2024-11-06

### Добавлено

- Классы для обработки сетевых ошибок:
  - `AxiosNetworkAppError` (для `axios`).
  - `SuperagentNetworkAppError` (для `superagent` и `OpenAPI Middleware`).
- Класс `LogicAppError` для бизнес-логики приложения.

### Изменено

- **BREAKING CHANGE**: Переход на сборку через `Vite` (вместо Babel).
- Рефакторинг базового класса `AppError` (теперь не содержит логики парсинга).

## [1.0.0] - 2023-09-28

### Добавлено

- Базовый класс `AppError` с парсингом HTTP-ошибок.
- Заглушки для интеграции:
  - `AppErrorViaAxios` (требует реализации).
  - `AppErrorViaSuperagent` (аналогично).
- Первоначальная документация.

[3.3.0]: https://github.com/andrey-aka-skif/app-errors/compare/v3.2.3...v3.3.0
[3.2.3]: https://github.com/andrey-aka-skif/app-errors/compare/v3.2.2...v3.2.3
[3.2.2]: https://github.com/andrey-aka-skif/app-errors/compare/v3.2.1...v3.2.2
[3.2.1]: https://github.com/andrey-aka-skif/app-errors/compare/v3.2.0...v3.2.1
[3.2.0]: https://github.com/andrey-aka-skif/app-errors/compare/v3.1.6...v3.2.0
[3.1.6]: https://github.com/andrey-aka-skif/app-errors/compare/v3.1.3...v3.1.6
[3.1.3]: https://github.com/andrey-aka-skif/app-errors/compare/v3.1.0...v3.1.3
[3.1.0]: https://github.com/andrey-aka-skif/app-errors/compare/v3.0.0...v3.1.0
[3.0.0]: https://github.com/andrey-aka-skif/app-errors/compare/v2.0.5...v3.0.0
[2.0.0]: https://github.com/andrey-aka-skif/app-errors/compare/v1.0.0...v2.0.0
[1.0.0]: https://github.com/andrey-aka-skif/app-errors/releases/tag/v1.0.0
